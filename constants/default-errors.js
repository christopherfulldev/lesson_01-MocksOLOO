'use strict';
const DEFAULT_ERRORS = {
    errors: {
        FILE_LENGTH_ERROR_MESSAGE: 'The content length is invalid!',
        FILE_FIELDS_ERROR_MESSAGE: 'The provide properties are invalid!'
    }
};

module.exports = DEFAULT_ERRORS;